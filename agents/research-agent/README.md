# Research Agent

Optional HTTP worker for deep web and market research. The Phase 3 scaffold
does not bundle a crawler. Configure a trusted backend later with
`RESEARCH_AGENT_BACKEND_URL`.

```bash
docker compose --profile research up -d
```

