version: "3.9"

services:
  openclaw:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        http_proxy: ${HTTP_PROXY:-}
        https_proxy: ${HTTPS_PROXY:-}
        no_proxy: ${NO_PROXY:-}

    image: native-openclaw:latest
    container_name: native-openclaw

    stdin_open: true
    tty: true

    env_file:
      - .env

    environment:
      APP_ENV: production
      NODE_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-info}

      APP_DATA_DIR: /data
      SKILLS_DIR: /skills
      WORKSPACE_DIR: /workspace
      WORKFLOW_FILE: /workspace/WORKFLOW.md
      TOOLS_DIR: /app/tools
      MCP_CONFIG_PATH: /data/mcp.json
      STORAGE_BACKEND: file

      NODE_OPTIONS: "--enable-source-maps"

      HTTP_PROXY: ${HTTP_PROXY:-}
      HTTPS_PROXY: ${HTTPS_PROXY:-}
      NO_PROXY: ${NO_PROXY:-localhost,127.0.0.1,::1,openclaw,ollama,host.docker.internal,.local,.internal}
      http_proxy: ${HTTP_PROXY:-}
      https_proxy: ${HTTPS_PROXY:-}
      no_proxy: ${NO_PROXY:-localhost,127.0.0.1,::1,openclaw,ollama,host.docker.internal,.local,.internal}

      npm_config_proxy: ${HTTP_PROXY:-}
      npm_config_https_proxy: ${HTTPS_PROXY:-}
      npm_config_noproxy: ${NO_PROXY:-localhost,127.0.0.1,::1,openclaw,ollama,host.docker.internal,.local,.internal}

      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}

      WEB_UI_ENABLED: ${WEB_UI_ENABLED:-false}
      WEB_UI_HOST: ${WEB_UI_HOST:-0.0.0.0}
      WEB_UI_PORT: ${WEB_UI_PORT:-18790}
      WEB_UI_USERNAME: ${WEB_UI_USERNAME:-admin}
      WEB_UI_PASSWORD: ${WEB_UI_PASSWORD:-change-me}
      WEB_UI_SESSION_SECRET: ${WEB_UI_SESSION_SECRET:-change-this-secret}

    volumes:
      - ./data:/data
      - ./skills:/skills
      - ./workspace:/workspace

    ports:
      - "${API_PORT:-18789}:18789"
      - "${WEB_UI_PORT:-18790}:18790"

    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: unless-stopped
